\documentclass[twoside, 12pt]{article}
\usepackage{amsmath}
\usepackage{graphicx, xdipp, url, fancyvrb}
\usepackage[hidelinks]{hyperref}
\english
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  numbers=left,
  numberstyle=\tiny,
  language=Python
}
\usepackage{caption}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{float}

\begin{document}

\makeatletter
\def\typprace{Semester}
\def\c@prace{project}
\def\a@prace{project}
\renewcommand{\contentsname}{Contents}
\renewcommand{\refname}{References}
\def\obsah{\cleardoublepage\vspace*{5mm}%
  \noindent {\doplsanserif\bfseries Abstract}\par\medskip
  \noindent \@enabstraktdata\par\medskip\noindent\@enabstrakttext
  \if .\@enklslova\else \par\medskip\noindent
       {\doplsanserif\bfseries Key words:} \@enklslova
  \fi
  \cleardoublepage\tableofcontents\cleardoublepage}
\makeatother
\titul{Sentiment Classification, Topic Clustering and Similarity Search in Hotel Reviews}
{\begin{tabular}[b]{@{}r} Bc. Jan Dub \\ Bc. David Krčmář \\ Bc. René Mrákava \end{tabular}}{doc. Ing. František Dařena, Ph.D.}{Brno 2026}

\abstract{Dub, J.; Krčmář, D.; Mrákava, R. Sentiment Classification, Topic Clustering and Similarity Search in Hotel Reviews. Semester project. Mendel University in Brno, 2026.}
{This semester project presents a compact end-to-end text-mining pipeline for hotel reviews from the Kaggle dataset \emph{515K Hotel Reviews Data in Europe}. The work validates and summarizes the original CSV file, preprocesses review text, trains and compares three classical classifiers, evaluates chi-square feature selection, clusters negative reviews with K-Means, and builds a TF-IDF retrieval module with cosine similarity. The dataset contains 515,738 reviews and 17 columns. In the final benchmark run, sentiment classification used the full available dataset with \textbf{TF-IDF unigrams and bigrams, \texttt{SelectKBest(chi2, k=20000)} and Linear SVM}, reaching \textbf{macro F1 = 0.8510} and \textbf{accuracy = 95.90\%}. Negative-review clustering on a 50,000-row sample remains interpretable but weakly separated, with \textbf{k = 7} and \textbf{silhouette score = 0.0085}. Retrieval works well for specific multi-word queries and degrades for generic queries. The report discusses both the practical usefulness and the limits of classical text-mining methods on noisy user-generated review data.}

\keywords{text mining, hotel reviews, sentiment classification, clustering, information retrieval, TF-IDF, Linear SVM}

\obsah

\section*{\LARGE\textbf{Glossary}}

\begin{itemize}
    \item \textbf{\textit{TF-IDF}}: a weighted text representation that highlights terms important for a specific document while down-weighting terms that are frequent across the whole collection.
    \item \textbf{\textit{n-gram}}: a sequence of $n$ tokens; this project compares unigrams with a combination of unigrams and bigrams.
    \item \textbf{\textit{Linear SVM}}: a linear Support Vector Machine classifier suitable for sparse text representations with many features.
    \item \textbf{\textit{Chi-square feature selection}}: a feature-selection method based on the statistical dependence between a term and the target class.
    \item \textbf{\textit{K-Means}}: a clustering algorithm that partitions documents into a predefined number of clusters according to their distance from cluster centroids.
    \item \textbf{\textit{Silhouette score}}: an internal clustering metric that evaluates how compact a cluster is and how well it is separated from other clusters.
    \item \textbf{\textit{Cosine similarity}}: a vector similarity measure based on the angle between two vectors; it is a standard choice for comparing TF-IDF representations.
\end{itemize}

\kapitola{Introduction and motivation}

Hotel reviews are a suitable example of real-world text data on which the main tasks of text mining can be demonstrated within one shared pipeline. Each review combines free text, a numerical score, and additional metadata. This combination makes it possible to address sentiment classification, topic clustering, and similar-document search at the same time.

The practical motivation is twofold. First, hotel reviews are a commercially important source of feedback for accommodation providers and booking platforms such as Booking.com. Second, the dataset contains realistic noise: placeholders such as \texttt{No Negative}, very short entries, variable review length, and an imbalanced distribution of target classes. The project is therefore useful not only for demonstrating applicable methods, but also for discussing their limits openly.

From the perspective of the course, the work covers text representation, preprocessing, classification, feature selection, clustering, and information retrieval, which are standard tasks in text mining and NLP [4, 5]. Instead of focusing on one isolated task, the project creates a consistent text-mining workflow with a shared dataset and a common evaluation logic.

\kapitola{Project goal and research questions}

The goal of this project is to design and implement a compact text-mining pipeline for hotel-review analysis and to evaluate which classical methods work best on this type of data.

\sekce{Main objectives}

\begin{enumerate}
    \item prepare and document a hotel-review dataset,
    \item create a shared text representation suitable for machine learning,
    \item train and compare several sentiment-classification models,
    \item evaluate the effect of feature selection using \texttt{SelectKBest(chi2)},
    \item identify the main themes in negative reviews using clustering,
    \item create a simple retrieval module returning similar reviews for a given query.
\end{enumerate}

\sekce{Research questions}

\begin{enumerate}
    \item Which of the three classical models (\texttt{MultinomialNB}, \texttt{LogisticRegression}, \texttt{LinearSVC}) achieves the best performance on hotel reviews?
    \item Does feature selection using \texttt{SelectKBest(chi2)} improve performance compared with a plain TF-IDF baseline?
    \item Which themes dominate negative reviews, and how interpretable are the resulting clusters?
    \item How well does TF-IDF and cosine-similarity retrieval work for specific and generic queries?
\end{enumerate}

\kapitola{Dataset}

\sekce{Data source and structure}

The primary data source is the Kaggle dataset \emph{515K Hotel Reviews Data in Europe} by Jiashen Liu [1]. The project uses the original file \texttt{Hotel\_Reviews.csv}, which is expected in the local project structure under \texttt{data/raw/}. The dataset contains the text fields \texttt{Positive\_Review} and \texttt{Negative\_Review}, the numerical score \texttt{Reviewer\_Score}, and additional metadata such as \texttt{Hotel\_Name}, \texttt{Average\_Score}, \texttt{Tags}, and the hotel location.

\begin{table}[H]
\centering
\caption{Basic dataset characteristics}
\begin{tabular}{|p{5.2cm}|p{8cm}|}
\hline
\textbf{Item} & \textbf{Value} \\ \hline
Source & Kaggle: \emph{515K Hotel Reviews Data in Europe} \\ \hline
Main file & \texttt{Hotel\_Reviews.csv} \\ \hline
Number of rows & 515,738 \\ \hline
Number of columns & 17 \\ \hline
Key attributes & \texttt{Positive\_Review}, \texttt{Negative\_Review}, \texttt{Reviewer\_Score}, \texttt{Hotel\_Name}, \texttt{Tags} \\ \hline
Missing values & 0 in the review-text fields and score; 3,268 missing values in \texttt{lat}/\texttt{lng} \\ \hline
\texttt{Reviewer\_Score} range & 2.5 to 10.0 \\ \hline
Mean / median score & 8.3951 / 8.8 \\ \hline
\end{tabular}
\end{table}

\sekce{Data quality and practical limitations}

The dataset is large, but it is not clean. Many reviews use placeholders such as \texttt{No Negative} or \texttt{No Positive}. In the verified local snapshot, there are 152,663 values of \texttt{No Negative} and 37,791 values of \texttt{No Positive}. In addition, short pseudo-negative expressions such as \texttt{No complaints}, \texttt{Everything was perfect}, or simply \texttt{Breakfast} occur in the data. These are not technical placeholders, but they complicate further processing.

Another limitation is the strongly positive score distribution. The most frequent value in the full dataset is 10.0, appearing 115,853 times, while the lower part of the scale is much less common. This is important for label design and for interpreting accuracy, because accuracy alone is not sufficient for an imbalanced classification problem.

\sekce{Review-score distribution}

The distribution of \texttt{Reviewer\_Score} is shown in Figure \ref{fig:score-distribution}. The high concentration of scores between 8.3 and 10.0 confirms that the dataset has a natural positive bias.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{outputs/visuals/reviewer_score_distribution.png}
\caption{Distribution of \texttt{Reviewer\_Score} in the full dataset}
\label{fig:score-distribution}
\end{figure}

\sekce{Working mode}

The project uses different dataset scopes for different tasks. Classification and the final retrieval query were run on the full available dataset without downsampling. Clustering remains sample-based with 50,000 records, because full-dataset clustering is slower, harder to interpret, and does not automatically improve cluster separation. 
This compromise fits the educational nature of the project: the main goal is to compare methods and interpret results, not to maximize model performance at the cost of long runtime and higher memory requirements.

\sekce{Reproducibility note}

The dataset is not included in the repository because of its size and distribution through Kaggle. After downloading \texttt{Hotel\_Reviews.csv} into \texttt{data/raw/}, the final benchmark and the supporting report figures can be reproduced with the following commands:

\begin{lstlisting}[basicstyle=\ttfamily\scriptsize, numbers=none, frame=single, breaklines=true]
python src/classification.py --data data/raw/Hotel_Reviews.csv --full-dataset --use-bigrams --feature-selection-k 20000
python src/feature_selection_experiments.py --data data/raw/Hotel_Reviews.csv --sample-size 50000 --use-bigrams
python src/clustering.py --data data/raw/Hotel_Reviews.csv --sample-size 50000 --k 7 --use-bigrams
python src/clustering_experiments.py --data data/raw/Hotel_Reviews.csv --sample-size 50000 --k-values 5,6,7,8,9 --use-bigrams --output-dir outputs/clustering_50k_comparison
python src/retrieval.py --data data/raw/Hotel_Reviews.csv --full-dataset --query "dirty room and noisy street" --top-k 5
python src/retrieval_experiments.py --data data/raw/Hotel_Reviews.csv --sample-size 20000 --use-bigrams
python src/generate_report_figures.py --data data/raw/Hotel_Reviews.csv --clustering-comparison outputs/clustering_50k_comparison/clustering_k_comparison.csv
\end{lstlisting}

\kapitola{Data preprocessing}

\sekce{Text-attribute construction}

The project creates three shared text variants:

\begin{itemize}
    \item \texttt{review\_text}: concatenation of the positive and negative parts of the review,
    \item \texttt{negative\_text}: only the negative part of the review,
    \item \texttt{positive\_text}: only the positive part of the review.
\end{itemize}

For sentiment classification, \texttt{review\_text} proved to be the best option because it combines both sides of the review and gives the model the most context. For clustering, \texttt{negative\_text} was used because problem topics are more interpretable in that field.

\sekce{Cleaning pipeline}

Before text representation, the following steps were applied:

\begin{enumerate}
    \item replace placeholders in \texttt{Positive\_Review} and \texttt{Negative\_Review} with empty text,
    \item remove records with empty selected text fields,
    \item convert text to lowercase,
    \item remove non-alphabetic characters,
    \item normalize whitespace,
    \item remove records that remained empty after cleaning.
\end{enumerate}

On the full available dataset, binary label filtering and preprocessing produced the following data flow:

\begin{table}[H]
\centering
\caption{Data flow after labeling and preprocessing}
\begin{tabular}{|p{7cm}|r|}
\hline
\textbf{Step} & \textbf{Number of rows} \\ \hline
Loaded dataset & 515,738 \\ \hline
After binary labeling & 366,349 \\ \hline
After filtering empty text fields & 365,997 \\ \hline
After final cleaning & 365,988 \\ \hline
Training split & 292,790 \\ \hline
Test split & 73,198 \\ \hline
\end{tabular}
\end{table}

\sekce{Text representation and labels}

The final classification baseline uses the TF-IDF representation from scikit-learn [2]. TF-IDF is a standard sparse vector-space representation for text retrieval and related text-mining tasks [3]. Both unigrams and a combination of unigrams and bigrams were tested; the second option performed better. The selected configuration was:

\begin{itemize}
    \item \texttt{ngram\_range = (1, 2)},
    \item \texttt{min\_df = 5},
    \item \texttt{max\_df = 0.9},
    \item \texttt{max\_features = 50000},
    \item the English stop-word list from scikit-learn.
\end{itemize}

Sentiment labels were created as binary labels:

\begin{itemize}
    \item \texttt{Reviewer\_Score <= 5.0} $\rightarrow$ negative,
    \item \texttt{Reviewer\_Score >= 8.0} $\rightarrow$ positive,
    \item the middle score range was excluded from classification.
\end{itemize}

This design intentionally increases class separability, but it also reduces the number of usable records. After filtering, the full benchmark dataset contained 335,365 positive and 30,623 negative reviews, confirming strong class imbalance.

\kapitola{Sentiment classification}

\sekce{Models and evaluation setup}

Three classical models suitable for sparse text representations were compared. The implementation uses scikit-learn [2]; Linear SVM follows the margin-based classification principle introduced by support vector machines [6].

\begin{itemize}
    \item \texttt{Multinomial Naive Bayes},
    \item \texttt{Logistic Regression},
    \item \texttt{Linear SVM}.
\end{itemize}

The data was split into stratified training and test sets in an 80/20 ratio. Because of class imbalance, accuracy is reported together with macro precision, macro recall, and macro F1. This is important because high accuracy could otherwise hide weak performance on the minority negative class.

\sekce{Final benchmark results with feature selection}

Table \ref{tab:classification-baseline} summarizes the final benchmark results on \texttt{review\_text} using TF-IDF unigrams and bigrams with \texttt{SelectKBest(chi2, k=20000)}.

\begin{table}[H]
\centering
\caption{Final sentiment-classification results with \texttt{SelectKBest(chi2, k=20000)}}
\label{tab:classification-baseline}
\small
\begin{tabular}{|p{4.0cm}|c|c|c|c|}
\hline
\textbf{Model} & \textbf{Macro prec.} & \textbf{Macro rec.} & \textbf{Macro F1} & \textbf{Acc.} \\ \hline
Linear SVM & 0.8977 & 0.8158 & 0.8510 & 0.9590 \\ \hline
Logistic Regression & 0.9131 & 0.7938 & 0.8412 & 0.9582 \\ \hline
Multinomial Naive Bayes & 0.9044 & 0.7726 & 0.8230 & 0.9544 \\ \hline
\end{tabular}
\normalsize
\end{table}

Linear SVM ranked first in the final benchmark. Logistic Regression remained competitive, but it lagged mainly in macro recall. Naive Bayes achieved acceptable accuracy, but it performed worse on the minority negative class, which reduced macro F1.

\sekce{Precision-Recall and ROC comparison}

Because the negative class is much smaller than the positive class, the Precision-Recall curve in Figure \ref{fig:classification-pr} uses \emph{negative review} as the positive event. This makes the plot directly focused on the practically important task: finding dissatisfied customers. Logistic Regression has the highest ranking-based average precision (AP = 0.812), while Linear SVM has the best thresholded macro F1 in Table \ref{tab:classification-baseline}. The difference shows that ranking quality and the final hard-label decision are related, but not identical, evaluation views.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{outputs/visuals/classification_pr_curve.png}
\caption{Precision-Recall curves for negative-review detection}
\label{fig:classification-pr}
\end{figure}

The ROC curves in Figure \ref{fig:classification-roc} are included as a supplementary view. All three models achieve high ROC-AUC values above 0.95, but ROC-AUC is less sensitive to the strong class imbalance than the Precision-Recall view.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{outputs/visuals/classification_roc_curve.png}
\caption{ROC curves for negative-review detection}
\label{fig:classification-roc}
\end{figure}

\sekce{Interpretation of the best final model}

For the best final model (\texttt{Linear SVM}), the confusion matrix shows the following behavior:

\begin{table}[H]
\centering
\caption{Confusion matrix for final Linear SVM}
\begin{tabular}{|l|c|c|}
\hline
\textbf{Actual / predicted class} & \textbf{Negative} & \textbf{Positive} \\ \hline
Negative & 3,944 (64.39\%) & 2,181 (35.61\%) \\ \hline
Positive & 823 (1.23\%) & 66,250 (98.77\%) \\ \hline
\end{tabular}
\end{table}

The model recognizes positive reviews very well, but it still assigns part of the negative reviews to the positive class. Out of 6,125 negative test reviews, it correctly identifies 3,944 reviews (64.39\%) and incorrectly marks 2,181 reviews (35.61\%) as positive. These percentages are the most important part of the confusion matrix, because negative reviews incorrectly predicted as positive are false negatives for negative-review detection: problematic stays are missed. This behavior corresponds to the nature of the data: the negative class is smaller, lexically less homogeneous, and more likely to contain short or mixed formulations.

\kapitola{Feature selection}

\sekce{Method and experimental setup}

In the next phase, \texttt{SelectKBest(chi2)} from scikit-learn was applied to the same TF-IDF representation [2]. The goal was to test whether feature selection can remove part of the noise, improve generalization, and provide more interpretable terms for sentiment analysis.

Feature selection was first compared on a 50,000-row development sample with $k \in \{0, 5000, 10000, 20000, 30000\}$, where $k = 0$ means the baseline without feature selection. For that smaller experiment, the actual number of active features after vectorization was only 15,266; therefore, the configurations with $k = 20000$ and $k = 30000$ effectively retained all features and reproduced the baseline.

\sekce{Configuration comparison}

\begin{table}[H]
\centering
\caption{Effect of feature selection on the best model (Linear SVM)}
\begin{tabular}{|c|c|c|}
\hline
\textbf{k} & \textbf{Macro F1} & \textbf{Accuracy} \\ \hline
0 & 0.8039 & 0.9481 \\ \hline
5,000 & 0.8120 & 0.9503 \\ \hline
10,000 & 0.8032 & 0.9479 \\ \hline
20,000 & 0.8039 & 0.9481 \\ \hline
30,000 & 0.8039 & 0.9481 \\ \hline
\end{tabular}
\end{table}

The same comparison is visualized in Figure \ref{fig:feature-selection}. The best development-sample result was obtained with \texttt{k = 5000}. For the final full-dataset benchmark, the project used \texttt{k = 20000} to retain more potentially useful terms in the much larger corpus while still applying chi-square feature selection. The graph also shows that the differences are relatively small: feature selection helps, but it is not the main source of the classification performance.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{outputs/visuals/feature_selection_linear_svm.png}
\caption{Comparison of the Linear SVM baseline and selected values of $k$}
\label{fig:feature-selection}
\end{figure}

\sekce{Most important sentiment terms}

Feature selection also supports interpretation. For the final configuration (\texttt{Linear SVM + k = 20000}), the strongest indicators included the following terms:

\begin{table}[H]
\centering
\caption{Selected sentiment terms}
\begin{tabular}{|p{5.8cm}|p{5.8cm}|}
\hline
\textbf{Positive sentiment} & \textbf{Negative sentiment} \\ \hline
excellent & dirty \\ \hline
amazing & worst \\ \hline
lovely & disinterested \\ \hline
fantastic & filthy \\ \hline
great & unclean \\ \hline
loved & impolite \\ \hline
perfect & rude \\ \hline
definitely stay & unhelpful \\ \hline
\end{tabular}
\end{table}

The terms are semantically interpretable and confirm that classical linear models over TF-IDF provide a meaningful signal for this type of data.

\kapitola{Clustering negative reviews}

\sekce{Input data and method}

Clustering was performed on \texttt{negative\_text}, because this field is the most useful for identifying repeated problem types. From a 50,000-row sample, 35,088 documents remained after removing empty and uninformative texts. TF-IDF with unigrams and bigrams was used, followed by K-Means from scikit-learn [2]. K-Means is a standard partitional clustering method [7].

\sekce{Choosing the number of clusters}

Several values of $k$ were compared on the same 50,000-row clustering sample. The comparison is summarized in Table \ref{tab:clustering-k-comparison} and visualized in Figure \ref{fig:clustering-k-comparison}.

\begin{table}[H]
\centering
\caption{K-Means comparison for different values of $k$}
\label{tab:clustering-k-comparison}
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{k} & \textbf{Silhouette score} & \textbf{Min. cluster size} & \textbf{Max. cluster size} \\ \hline
5 & 0.0070 & 1,087 & 27,447 \\ \hline
6 & 0.0079 & 1,614 & 16,839 \\ \hline
7 & 0.0085 & 366 & 19,397 \\ \hline
8 & 0.0114 & 152 & 20,100 \\ \hline
9 & 0.0082 & 309 & 21,553 \\ \hline
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{outputs/visuals/clustering_k_comparison.png}
\caption{Silhouette-score comparison for K-Means clustering}
\label{fig:clustering-k-comparison}
\end{figure}

The silhouette score is an internal measure of cluster compactness and separation [8]. All tested values are very close to zero, so the result must be interpreted carefully: the clusters are interpretable, but they are not sharply separated. The highest silhouette score in this run was obtained for $k = 8$, but that configuration created a very small pseudo-negative cluster dominated by terms such as \texttt{perfect} and \texttt{just perfect}, while still keeping one very large mixed cluster. The final choice $k = 7$ is therefore a practical compromise: it keeps the solution manageable, avoids an extra tiny artificial cluster, and still separates several useful hotel-problem themes such as Wi-Fi/air conditioning, small rooms, breakfast, and parking.

\sekce{Interpretation of the final clusters}

For the selected final configuration, the clusters were manually named according to top terms and representative reviews:

\begin{table}[H]
\centering
\caption{Manual interpretation of clusters for $k = 7$}
\begin{tabular}{|c|r|p{6cm}|}
\hline
\textbf{Cluster} & \textbf{Size} & \textbf{Interpretation} \\ \hline
0 & 366 & generic dislike or short negative fragments \\ \hline
1 & 8,282 & room comfort, service, bathroom, and night issues \\ \hline
2 & 1,435 & Wi-Fi and air-conditioning problems \\ \hline
3 & 19,397 & broad miscellaneous hotel and staff comments \\ \hline
4 & 2,016 & small-room complaints \\ \hline
5 & 2,980 & breakfast quality, price, and inclusion \\ \hline
6 & 612 & parking and car-access costs \\ \hline
\end{tabular}
\end{table}

The cleanest clusters are those focused on Wi-Fi, air conditioning, small rooms, breakfast, and parking. The weakest result is the largest cluster, which captures a mixture of broad hotel, staff, room, and bathroom comments.

\sekce{Weaknesses of clustering}

The low silhouette score and the existence of a large ``miscellaneous'' cluster are directly related to input-text quality. The data still contains short or pseudo-negative formulations such as \texttt{No failings}, \texttt{No complaints}, \texttt{Everything was perfect}, \texttt{Breakfast}, or \texttt{Expensive}. These expressions reduce thematic purity and distort centroids. Therefore, clustering should be understood mainly as an exploratory tool for summarizing frequent problems, not as a definitive segmentation model.

\kapitola{Information retrieval}

\sekce{Retrieval-module design}

The retrieval module uses the same vector-space principle as the classification representation, but it is applied to \texttt{review\_text}. In the final run, retrieval was configured for the full available dataset without downsampling. The TF-IDF representation uses up to 30,000 unigram features, and retrieval is performed using cosine similarity, a common similarity measure in vector-space information retrieval [3].

\sekce{Example queries}

The final reproducibility run used the query \texttt{dirty room and noisy street}. A separate multi-query development check also covered several specific and generic formulations:

\begin{itemize}
    \item \texttt{dirty room and noisy street},
    \item \texttt{friendly staff and great location},
    \item \texttt{small bathroom and old furniture},
    \item \texttt{excellent breakfast and comfortable bed},
    \item \texttt{slow wifi and poor service},
    \item \texttt{good hotel}.
\end{itemize}

\sekce{Similarity-score based assessment}

\begin{table}[H]
\centering
\caption{Retrieval-query comparison using cosine similarity}
\small
\begin{tabular}{|p{5.1cm}|c|c|p{3.6cm}|}
\hline
\textbf{Query} & \textbf{Top cosine} & \textbf{Mean top-5} & \textbf{Interpretation note} \\ \hline
friendly staff and great location & 1.0000 & 0.8233 & very close lexical matches \\ \hline
small bathroom and old furniture & 0.6613 & 0.5112 & concrete negative aspects \\ \hline
excellent breakfast and comfortable bed & 0.5636 & 0.5301 & two clear positive aspects \\ \hline
dirty room and noisy street & 0.5671 & 0.5184 & noise captured strongly \\ \hline
slow wifi and poor service & 0.6049 & 0.4904 & Wi-Fi signal dominates \\ \hline
good hotel & 1.0000 & 0.8592 & generic query, low specificity \\ \hline
\end{tabular}
\normalsize
\end{table}

The same scores are shown in Figure \ref{fig:retrieval-scores}. The cosine values should not be interpreted as manual relevance labels. They measure lexical similarity in TF-IDF space. This is why the generic query \texttt{good hotel} receives high cosine scores, even though it is less useful for analysis than a more specific query. Retrieval works best when the query contains concrete aspects of the hotel experience. In the final full-dataset run, the top results for \texttt{dirty room and noisy street} were short but relevant, including \texttt{very noisy dirty}, \texttt{room on the street side very noisy}, and \texttt{the room was very noisy from the street}. For generic queries, the model naturally prefers short and frequent positive phrases, which reduces the usefulness of the results.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{outputs/visuals/retrieval_query_scores.png}
\caption{Top and mean top-5 cosine similarity for retrieval queries}
\label{fig:retrieval-scores}
\end{figure}

\kapitola{Discussion}

\sekce{What worked best}

The most stable part of the pipeline was sentiment classification. In the final full-dataset benchmark, Linear SVM with TF-IDF and chi-square feature selection reached macro F1 = 0.8510 and accuracy = 95.90\%. This confirms that linear models over sparse representations remain strong and interpretable for this type of data.

The retrieval module also performed well. For specific multi-word queries, it returned meaningful and easily interpretable results without requiring more complex embedding models.

\sekce{What was problematic}

The main problem in the dataset is noise. In addition to explicit placeholders, the data contains short, semantically ambiguous, or only formally negative entries. This noise directly affects clustering, where it leads to weak separation and to one dominant miscellaneous cluster.

The second problem is class imbalance after binary labeling. Classification accuracy is very high, but without macro metrics it would easily hide the fact that the model still has substantially weaker recall on the negative class than on the positive class.

\sekce{Limitations of the work}

\begin{itemize}
    \item clustering was still run on a 50,000-row sample rather than on the full dataset,
    \item retrieval was evaluated with cosine scores and manual inspection, without a manually annotated relevance benchmark,
    \item clustering was interpreted manually and without external ground truth,
    \item preprocessing scope was intentionally kept simple.
\end{itemize}

\sekce{Future work}

Natural extensions of the project include:

\begin{itemize}
    \item expanding the placeholder dictionary and filtering short pseudo-negative phrases more precisely,
    \item running additional full-dataset feature-selection comparisons,
    \item comparing K-Means with NMF or topic modeling,
    \item extending retrieval with BM25 or sentence embeddings,
    \item trying class weighting or targeted sampling to improve recall for the negative class.
\end{itemize}

\kapitola{Conclusion}

The project showed that classical text-mining methods can produce solid and interpretable results on hotel reviews. After validating and cleaning the dataset, a unified pipeline was created for text representation, sentiment classification, feature selection, clustering, and retrieval.

The best final classification configuration uses \texttt{review\_text}, TF-IDF with unigrams and bigrams, \texttt{SelectKBest(chi2, k=20000)}, and Linear SVM. This configuration achieved macro F1 = 0.8510 and accuracy = 95.90\%. Clustering of negative reviews produced useful thematic summaries, especially for Wi-Fi, air conditioning, small rooms, breakfast, and parking, but the internal separation of clusters was weak. The retrieval module confirmed that TF-IDF and cosine similarity remain practically usable for specific queries.

Overall, the project met its intended goal: it demonstrated a complete text-mining workflow on one realistic dataset, including both strengths and limitations. The results are concrete enough for the written report and for project defense, while also leaving clear space for further extensions.

\kapitola{References}

{\raggedright
\begin{enumerate}
    \item Liu, J. \emph{515K Hotel Reviews Data in Europe}. Kaggle dataset. Available from: \url{https://www.kaggle.com}. Dataset path: \texttt{datasets/jiashenliu/515k-hotel-reviews-data-in-europe}.
    \item Pedregosa, F. et al. Scikit-learn: Machine Learning in Python. \emph{Journal of Machine Learning Research}, 12, 2011, pp.~2825--2830.
    \item Manning, C. D.; Raghavan, P.; Schütze, H. \emph{Introduction to Information Retrieval}. Cambridge University Press, 2008.
    \item Žižka, J.; Dařena, F.; Svoboda, A. \emph{Text Mining with Machine Learning: Principles and Techniques}. CRC Press, 2019.
    \item Jurafsky, D.; Martin, J. H. \emph{Speech and Language Processing}. 3rd ed. draft, 2026. Available from: \url{https://web.stanford.edu/~jurafsky/slp3/}.
    \item Cortes, C.; Vapnik, V. Support-vector networks. \emph{Machine Learning}, 20, 1995, pp.~273--297.
    \item MacQueen, J. Some methods for classification and analysis of multivariate observations. In: \emph{Proceedings of the Fifth Berkeley Symposium on Mathematical Statistics and Probability}, 1967, pp.~281--297.
    \item Rousseeuw, P. J. Silhouettes: A graphical aid to the interpretation and validation of cluster analysis. \emph{Journal of Computational and Applied Mathematics}, 20, 1987, pp.~53--65.
\end{enumerate}
\par}

\end{document}
